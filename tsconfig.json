{
  "compilerOptions": {
    "target": "es6",
    "outDir": "./dist",
    // "rootDir": "./src", // この設定があると、プロジェクト参照でビルドができない
    "module": "commonjs",
    "downlevelIteration": true,
    "strict": true,
    "noUnusedLocals": true,
    "esModuleInterop": true,
    "moduleResolution": "node",
    "lib": ["es6", "dom"],
    "types": ["reflect-metadata"], // InversifyJS の設定
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "baseUrl": ".", // This must be specified if "paths" is.
    "allowJs": true // これを入れないを参照先のプロジェクトを読み込めない
    // "paths": {
    //   "@common-lib-for-slack/*": ["../common-lib-for-slack/*"]
    // }
  },
  "include": ["./src/**/*"],
  "exclude": ["node_modules", "**/*.test.ts"],
  // 共通ライブラリ使用側では以下を設定する。
  "references": [
    {
      "path": "../common-lib-for-slack"
      // "prepend": true
    }
  ]
}
